f-strings
len() - can give you letter by letter for word OR if it's a list of dictionaries it will give you the number of dict inside that list
get() - same as if I would do dict["key"] in dictionary
max() - gets the max value but if adding , key= we can point on the criteria or on what it should perfrom max
enumerate() - assigns the numbers to values
.items() - so called explode. if we have list of dictionaries we can use it to iterate in for loop over each of the lists
lambda - A lambda is a mini function you write in one line without giving it a name.
