Commands I learned

1. git status - checking the status of files added
2. git add - adding files to load on the github
3. git commit -m - commiting files to github and adding a message to them
4. git push - pushing files to github
5. git checkout branch - switching the brunches
6. git branch - check the branch list and the current branch selected
7. git config --list - returns so called list. the lists are like variables that you can reffer
8. git branch -c brunch-name - creates branch
